{% extends 'base.html.twig' %}

{% block title %}Edit Resident{% endblock %}

{% block body %}

    <h1>Edit Resident</h1>
    {{ include('resident/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="{{ path('app_resident_index') }}" class="btn btn-secondary mt-2">Back to list</a> <!-- Bouton "Back to list" stylisé -->
    {{ include('resident/_delete_form.html.twig') }}
{% endblock %}
