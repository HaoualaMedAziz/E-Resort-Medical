{% extends 'base.html.twig' %}

{% block title %}Edit Resident{% endblock %}

{% block body %}
    <h1>Edit Resident</h1>

    <div class="container-fluid mt-4"> <!-- Ajout de la classe mt-4 pour une marge en haut -->
        <div class="row">
            <div class="col-md-6"> <!-- Utilisation de la grille Bootstrap pour la mise en page -->
                {{ include('resident/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="col-md-6"> <!-- Utilisation de la grille Bootstrap pour la mise en page -->
                {{ include('resident/_delete_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('app_resident_index') }}" class="btn btn-secondary mt-3">Back to list</a> <!-- Bouton "Back to list" stylisé -->
    </div>
{% endblock %}
