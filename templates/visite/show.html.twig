{% extends 'base.html.twig' %}

{% block title %}Visite{% endblock %}

{% block body %}
    <h1>Visite</h1>

    <table class="table table-bordered " 
            style="background:#D6FCFC">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visite.id }}</td>
            </tr>
            <tr>
                <th>Résident</th>
                <td>{{ visite.membreFamille.resident }}</td>
            </tr>
            <tr>
                <th>Membre de famille</th>
                <td>{{ visite.membreFamille }}</td>
            </tr>
            <tr>
                <th>DateVisite</th>
                <td>{{ visite.dateVisite ? visite.dateVisite|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ visite.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visite_index') }}" class="btn btn-secondary mt-2">back to list</a>

    <a href="{{ path('app_visite_edit', {'id': visite.id}) }}" class="btn btn-success    mt-2">Modifier</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
