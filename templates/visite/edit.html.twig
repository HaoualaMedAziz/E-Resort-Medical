{% extends 'base.html.twig' %}

{% block title %}Modifier visite{% endblock %}

{% block body %}
    <h1>Modifier visite</h1>

    {{ include('visite/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('app_visite_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
