{% extends 'base.html.twig' %}

{% block title %}New Observation{% endblock %}

{% block body %}

    <h1>Create new Observation</h1>

    <div class="container-fluid">
    {{ include('observation/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('app_observation_index') }}">back to list</a>
    </div>
{% endblock %}
